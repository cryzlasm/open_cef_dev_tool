name: Build libcef Dev Tool

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build on Windows with VS2013
    runs-on: windows-2019  # VS2013 is not available on windows-latest, using windows-2019 which has VS2019 but we'll use the VS2013 toolset

    strategy:
      matrix:
        platform: [x86, x64]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.1

    - name: Build with MSBuild (VS2013)
      run: |
        if ("${{ matrix.platform }}" -eq "x86") {
          msbuild libcef_dev_tool.sln /p:Configuration=Release /p:Platform=Win32 /p:PlatformToolset=v120 /m
        } else {
          msbuild libcef_dev_tool.sln /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v120 /m
        }

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: libcef-dev-tool-${{ matrix.platform }}
        path: bin/Release/**/*